<% provide(:title, "Edit hanzi") %>
<h1>Update <span class="hanzi"><%= @hanzi.character %></span></h1>

<div class="center">
  <div class="center">
    <%= form_for(@hanzi) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      
      <%= f.label :components %>
      <%= f.text_field :components %>

      <div>
	Here are some basic strokes for copy-pasting:<br/>
	<span class="big-char hanzi">㇀㇁㇂㇃㇄㇅㇆㇇㇈㇉㇊㇋㇌㇍㇎㇏一丨<U+31D2></span>
      </div>
      <br/>
      <div>
	Hanzis without further decomposinition:<br/>
	<% @atoms.each do |a| %>
	  <span class="big-char hanzi"><%= a.character %></span>
	<% end %>
      </div>
      <br/>

      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
